#include<iostream>
#include<vector>
using namespace std;

int main()
{
    // vectors <char> vowels;       //completely empty vector - all of the alloted memory is innitialised to 0
    // vectors <char> vowels (5);           // 5 character long vector - all of them innitialised to 0
    vectors <char> vowels {'a','e','i','o','u'};        //initialising the vector list
 
    cout<<vector[0]<<endl;
    cout<<vector[4]<<endl;
    
}